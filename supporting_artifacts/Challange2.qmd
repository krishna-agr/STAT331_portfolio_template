---
title: "Challenge Week 2"
author: "Krishna Agrawal"
format:
  html:
    self-contained: true
    code-tools: true
    code-fold: true
execute:
  echo: true
  error: true
---

# Setup

```{r setup}
library(tidyverse)
library(here)
```

# Load

```{r}
library(ggridges)
library(tidyverse)
library(here)
surveys <- read_csv(here("Week#2", "Lab_Week_2", "surveys.csv"))
```

# Medium Challenge - Ridge Plots

```{r}

ggplot(data = surveys, mapping = aes(y = species, x = weight, fill = species)) +
  geom_density_ridges() +
  labs(y = "Species of Animal",
       x = "Weight (grams)", 
       title = "Weight in grams vs Species of Animal")   

```
